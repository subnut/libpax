Tests use minunit[1], which is automatically downloaded using curl[2].
So, ensure curl[2] is installed before running tests.

To run tests, execute -
	make tests

[1]: https://github.com/siu/minunit
[2]: https://curl.se
